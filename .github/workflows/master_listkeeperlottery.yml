# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More GitHub Actions for Azure: https://github.com/Azure/actions
name: Build and deploy Node.js app to Azure Web App - listkeeperLottery
name: Build and deploy Node.js app to Azure Web App - listkeeperLottery
on:
on:
  push:
  push:
    branches:
    branches:
      - master
      - master
  workflow_dispatch:
  workflow_dispatch:
jobs:
jobs:
  build:
  build:
    runs-on: windows-latest
    runs-on: windows-latest
    steps:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/checkout@v2
      - name: Set up Node.js version
      - name: Set up Node.js version
        uses: actions/setup-node@v1
        uses: actions/setup-node@v1
        with:
        with:
          node-version: '18.x'
          node-version: '18.x'
      - name: npm install, build, and test
      - name: npm install, build, and test
        run: |
        run: |
          npm install
          npm install
          npm run build --if-present
          npm run build --if-present
          npm run test --if-present
          npm run test --if-present
      # Optional: print directory contents for debugging
      - name: Print directory contents
        run: dir
      
      - name: Upload artifact for deployment job
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v2
        uses: actions/upload-artifact@v2
        with:
        with:
          name: node-app
          name: node-app
          path: .
          path: .
  deploy:
  deploy:
    runs-on: ubuntu-latest
    runs-on: ubuntu-latest
    needs: build
    needs: build
    environment:
    environment:
      name: 'Production'
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    steps:
    steps:
      - name: Download artifact from build job
      - name: Download artifact from build job
        uses: actions/download-artifact@v2
        uses: actions/download-artifact@v2
        with:
        with:
          name: node-app
          name: node-app
      # Optional: print directory contents after downloading artifact
      - name: Print directory contents after artifact download
        run: ls -al
      - name: 'Deploy to Azure Web App'
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v2
        uses: azure/webapps-deploy@v2
        id: deploy-to-webapp
        id: deploy-to-webapp
        with:
        with:
          app-name: 'listkeeperLottery'
          app-name: 'listkeeperLottery'
          slot-name: 'Production'
          slot-name: 'Production'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_74F40E15139E4951AC9AD88F9DDDF705 }}
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_74F40E15139E4951AC9AD88F9DDDF705 }}
          package: ./node-app 
